<h1 class="bg-primary">Productos</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Descripci&oacute;n</th>
    <th>Valor</th>
  </tr>

  <% @products.each do |product| %>
    <tr>
      <td><%= product.name %></td>
      <td><%= product.description %></td>
      <td><%= product.value %></td>
    </tr>
    <% end %>
</table>
